/* global $ Backbone */
var app = app || {};

$(function() {
  // Start the App
  new app.AppView();
});

app.AppView = Backbone.View.extend({
  el: '#main',

  initialize: function() {

  },

  render: function() {

  }
});
